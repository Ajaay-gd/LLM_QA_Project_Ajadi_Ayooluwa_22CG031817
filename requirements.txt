# Core dependencies for both CLI and Web applications

# HTTP requests for API calls
requests==2.31.0

# Web Framework - Flask
Flask==3.0.0
Werkzeug==3.0.1

# WSGI server for production deployment
gunicorn==21.2.0

# CORS support (if needed for API)
Flask-Cors==4.0.0

# Environment variables management
python-dotenv==1.0.0

# Optional: For better text processing
nltk==3.8.1

# Optional: For Streamlit version (if you want both options)
# streamlit==1.31.0
